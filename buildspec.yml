version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      - pip install -r requirements.txt

  pre_build:
    commands:
      - echo Running tests...
      - pytest tests/ -v

  build:
    commands:
      - echo Build completed successfully

artifacts:
  files:
    - '**/*'